import {makeStandardRouteItems} from './utils'

export default {
    areaName: 'Flow',
    title: '工作流',
    items: {
        WorkFlow: [
            ...makeStandardRouteItems('流程设计')
        ] 
    }
}
